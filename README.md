# Scripts for working with Fedora CoreOS

A project for collating useful scripts that can be installed in the core level of CoreOS.

### installation:
```
sudo core self-install /usr/local/bin --link --confirm
```

### examples so far:
```
core shell
```
